# 视频号发布台 MVP 审核报告

> 审核人：Hermes Agent (default)
> 审核日期：2026-06-14
> 审核对象：Codex 设计的视频号发布台 MVP
> 真实项目根目录：E:\CodexProjects\videohao-content-factory
> 审核方式：只读代码审查 + 验证脚本执行

---

## 一、总体结论

**当前 MVP 能交付日常使用，但存在一个必须立即修复的安全门��缺陷。**

单包发布台（`videohao_publish_desk_latest.html`）代码量小（HTML 86 行 + JS 145 行 + CSS 242 行），结构清晰，确实解决了"一条内容发布前材料集中"的核心问题。但"打开视频号"按钮仅检查 MP4 文件是否存在，不检查必查清单是否全部勾选——这是一个可能导致用户跳过审核步骤直接发布的隐患。

---

## 二、最严重的 5 个问题

### P0-1：发布按钮不要求完成必查清单（安全风险）

**现象：** `publish_desk.js` 第 102 行 `openWechat.disabled = !status.video_ready`，只要 MP4 文件存在，"打开视频号"按钮就启用。用户可以不勾选"已完整预览"、"标题正文已核对"、"字幕安全区已确认"就直接打开微信视频号后台。

**影响：** 用户疲劳状态下（特别是晚上 21:00 后）可能直接点击打开视频号，跳过所有审核步骤。这与任务要求"最终发布必须人工确认"矛盾。

**依据：** 代码第 45-58 行的 checklist 逻辑只保存到 localStorage，第 101-107 行的 openWechat 逻辑完全不读取 checklist 状态。

### P0-2：综合工作台过于复杂，干扰主流程

**现象：** `app.js` 2832 行，`styles.css` 2085 行，综合工作台有 14 个主导航模块。而单包发布台才是日常使用的核心入口。

**影响：** 用户晚上打开系统时，不知道该用哪个页面。两个入口展示的状态可能不一致（localStorage vs JSON 文件）。综合工作台在 MVP 阶段是负担而非帮助。

### P0-3：清单勾选状态不写回权威数据源

**现象：** 必查清单勾选结果只保存在浏览器 localStorage，刷新页面后还在，但写不回任何 JSON 文件或发布包。

**影响：** 无法追踪"这条内容到底审了没"。如果有团队协作者，也无法共享审核状态。

### P0-4：复制操作无版本提示，可能复制旧文案

**现象：** 点击"复制"后只显示"已复制"，不显示当前内容的包 ID、日期、版本号。

**影响：** 如果用户开了多个标签页（比如昨天和今天的发布台），可能复制了旧版本的标题或正文。

### P0-5：视频门禁只检查文件存在，不检查视频质量

**现象：** `renderer.py` 第 79 行 `video_ready = bool(video_path and Path(video_path).exists())`，只看文件是否存在，不检查编码、分辨率、时长、音频、水印。

**影响：** 如果用户放了错误的 MP4（比如横屏的、带水印的、尺寸不对的），系统不会阻止，完全依赖人工目检。这不是致命问题，但应该在页面上醒目提醒。

---

## 三、必须立刻修改的内容（MVP 阶段）

### 修改 1：发布按钮绑定必查清单

**文件：** `preview/publish_desk_assets/publish_desk.js`

**改法：** 在 `openWechat` 的 click 事件中，检查所有 `required: true` 的 checklist 是否全部勾选。未全部勾选时弹出确认对话框，告知用户跳过了哪些必查项。

**不允许：** 改变其他现有功能的行为。

### 修改 2：复制操作显示版本信息

**文件：** `preview/publish_desk_assets/publish_desk.js`

**改法：** `showToast` 函数改为显示"已复制 [标题] · 2026-06-09 · 别把好心给错人"，让用户知道复制的是哪个版本。

### 修改 3：在发布台首页醒目标注"仅规则判断"

**文件：** `preview/videohao_publish_desk_latest.html` 或 `publish_desk.js`

**改法：** 在数据复盘区域添加一行："当前学习结论基于少量样本，仅为规则判断，不构成预测。"

---

## 四、可以后面再做的内容

1. 综合工作台导航精简——先保留核心模块，其余折叠或下线
2. 状态权威源统一——选定一个 JSON 文件作为唯一状态源
3. 素材授权强制字段——给每个素材增加来源、授权、人工确认状态
4. 视频质量门禁——FFprobe 检查分辨率、编码、时长
5. 通用构建器——合并各主题的专用 build 脚本
6. 统一测试入口——定义 `verify_publish_mvp` 总命令
7. Git 版本管理——纳入版本控制
8. 雷达到发布闭环——真实建立映射
9. 发布后统一登记入口——强制记录发布时间
10. 相对路径改造——降低环境绑定

---

## 五、建议的页面结构

### 主入口：单包发布台（日常使用）

```
┌─────────────────────────────────────────────┐
│  视频号发布台 │ 当前选题：别把好心给错人     │ 复制全部 │ 打开视频号 [禁用]│
├──────────┬──────────────────┬───────────────┤
│ 发布准备  │    成品确认      │  发布内容     │
│          │                  │              │
│ 状态：尚未完成 │ [视频预览]     │ 标题: XX (3/16)│
│ 下一步：  │ [安全区开关]     │ [复制]      │
│ 合成 MP4 │                  │              │
│          │ [抽帧预览]       │ 正文: XX     │
│ 必查清单:│                  │ [复制]      │
│ ☐ 已预览  │                  │              │
│ ☐ 已核对  │                  │ 话题: XX     │
│ ☐ 安全区  │                  │ [复制]      │
│ ☐ 音乐确认│                  │              │
│          │                  │ [复制整段文案]│
│ 路径:    │                  │              │
│ [折叠▼]  │                  │ ⚠ 仍需手动发布│
├──────────┴──────────────────┴───────────────┤
│  分镜 │ 素材 │ 提示词 │ 数据复盘              │
└─────────────────────────────────────────────┘
```

### 次入口：综合工作台（管理和复盘）

**建议：** 综合工作台保留，但默认不打开。只在需要批量管理、数据录入、规则检查时才访问。首页只放一个"今日待发布"卡片，点击跳转到单包发布台。

---

## 六、建议新增按钮

### 1. "必查清单全部勾选后才启用"提示

**位置：** 顶部状态栏右侧，"打开视频号"按钮旁边

**行为：** 当 MP4 存在但必查清单未全部勾选时，显示橙色警告："还有 2 项必查未勾选"，点击展开清单。

### 2. "跳过确认"二次弹窗

**位置：** 点击"打开视频号"时

**行为：** 如果必查清单未全部勾选，弹出确认框：
"您还有 [X] 项必查未勾选：
- [ ] 已完整预览成品视频
- [ ] 标题正文已核对
是否仍要打开视频号发布页？"
用户必须手动确认才能继续。

### 3. 复制版本号提示

**位置：** 每次点击复制后

**行为：** Toast 提示改为"已复制标题 · 别把好心给错人 · 2026-06-09"

---

## 七、发布前门禁规则

以下情况必须禁止进入"可发布"状态：

1. **无 MP4 文件**：`video_ready = false`，"打开视频号"按钮禁用
2. **标题超过 16 字**：构建时校验失败，页面标记红色
3. **标题含标点或空格**：同上
4. **标题含高风险关键词**（医疗/夸大/引流/虚假权威）：综合工作台 audit_rules.json 已定义，页面应标红
5. **素材路径为空**：图片目录不存在或为空
6. **水印提示词被忽略**：图片和视频提示词中明确禁止文字/Logo/水印，但如果用户手动修改了提示词，页面应提醒
7. **BGM 版权未确认**：音乐文件存在但无授权信息，页面应有黄色提醒

---

## 八、数据回流设计

### 最简单的人工录入方式

**方案：** 在单包发布台底部增加一个"发布后录入"折叠面板，包含：

```
发布后数据录入（可选）
├─ 发布时间：[自动填入当前日期]
├─ 2小时后录入：
│  ├─ 播放量：[输入框]
│  ├─ 点赞：[输入框]
│  └─ 转发：[输入框]
├─ 24小时后录入：
│  ├─ 播放量：[输入框]
│  ├─ 完播率：[输入框]
│  └─ 评论数：[输入框]
└─ [保存] 按钮 → 写入 data/videohao_published_metrics.json
```

**关键：** 不强制录入，但提供一个便捷的入口。用户可以在发布后直接回到这个页面填写，不需要打开综合工作台或运行脚本。

**写入方式：** 使用 `fetch` 向本地 `http-server` 发送 POST 请求，由 Python 脚本追加写入 JSON。或者简单起见，生成一个 `.json` 文件到 `data/metrics_inbox/` 目录， nightly 脚本自动处理。

---

## 九、给 Codex 的修改任务清单

### 任务 1：立即修复 - 发布按钮绑定必查清单

**目标：** "打开视频号"按钮在 MP4 存在但未完成必查清单时，弹出二次确认。

**修改范围：**
- `preview/publish_desk_assets/publish_desk.js`：在 `openWechat` click 事件中添加 checklist 检查逻辑

**不允许修改：**
- 不改变现有复制、预览、标签页功能
- 不改 CSS

**验收标准：**
1. MP4 不存在时，按钮禁用（现有行为不变）
2. MP4 存在且必查项全部勾选，按钮正常打开视频号
3. MP4 存在但必查项未全部勾选，弹出 `<dialog>` 确认框，列出未勾选项，用户确认后仍可打开

### 任务 2：体验优化 - 复制版本号提示

**目标：** 每次复制后显示版本信息，防止复制旧文案。

**修改范围：**
- `preview/publish_desk_assets/publish_desk.js`：修改 `showToast` 函数

**不允许修改：**
- 不改复制逻辑本身

**验收标准：**
1. 复制标题时 Toast 显示：`已复制「别把好心给错人」· 2026-06-09`
2. 复制正文时 Toast 显示：`已复制正文 · 2026-06-09`
3. 复制整段文案时 Toast 显示：`已复制整段文案 · 2026-06-09`

### 任务 3：安全门控 - 数据复盘区域标注"早期样本"

**目标：** 防止用户误将规则判断当预测。

**修改范围：**
- `preview/publish_desk_assets/publish_desk.js`：`renderMetrics` 函数开头添加标注

**不允许修改：**
- 不改数据读取逻辑

**验收标准：**
1. 当 metrics 为空时，显示：`暂无人工导入数据。`
2. 当 metrics 有数据时，在数据卡片上方显示：`⚠ 当前基于少量样本的规则判断，不构成预测。`

### 任务 4：数据回流 - 发布后简易录入面板

**目标：** 在单包发布台底部增加发布后数据录入折叠面板。

**修改范围：**
- `preview/videohao_publish_desk_latest.html`：在 `<main>` 末尾添加录入面板 HTML
- `preview/publish_desk_assets/publish_desk.js`：添加录入逻辑
- `scripts/videohao_publish_desk_renderer.py`：在模型中注入 `publish_time` 字段

**不允许修改：**
- 不改综合工作台数据录入逻辑
- 不改现有 metrics 导入脚本

**验收标准：**
1. 页面底部新增"发布后录入"折叠面板
2. 包含 2h / 24h / 7d 三个时间节点的输入框
3. 点击保存后，生成 JSON 文件到 `data/metrics_inbox/` 目录
4. 文件名格式：`{content_id}_{time_node}.json`

### 任务 5：验收标准 - 运行验证脚本

**目标：** 确保所有修改不破坏现有验证。

**修改范围：**
- 运行 `scripts/verify_videohao_publish_desk.py`

**验收标准：**
1. 验证脚本 5 项全部通过
2. 新增的 checklist 检查逻辑不影响无视频状态
3. 新增的 Toast 信息不包含敏感票据

---

## 十、最终验收标准

### 功能验收

- [ ] 用户能在 3 分钟内完成一条视频的发布前检查
- [ ] 所有标题自动检测 16 字以内无标点
- [ ] 所有关键文案都有一键复制
- [ ] 复制后 Toast 显示版本信息（日期 + 标题）
- [ ] 无 MP4 时"打开视频号"按钮禁用
- [ ] 有 MP4 但未完成必查清单时，点击按钮弹出二次确认
- [ ] 水印素材不能通过门禁（提示词中已标注）

### 安全验收

- [ ] 不存在自动发布功能
- [ ] 不存在抓取微信后台数据的功能
- [ ] 不存在绕过登录/验证码的功能
- [ ] 页面不含 pass_ticket、exportkey 等临时票据
- [ ] 数据复盘区域标注"早期样本，仅规则判断"

### 数据回流验收

- [ ] 发布后可在单包页面直接录入数据
- [ ] 录入数据写入 `data/metrics_inbox/` 目录
- [ ] 文件名包含 content_id 和时间节点
- [ ] nightly 脚本能自动读取并汇入 `videohao_published_metrics.json`

### 代码质量验收

- [ ] 验证脚本 `verify_videohao_publish_desk.py` 5 项全部通过
- [ ] 单包发布台 HTML 不超过 120 行
- [ ] 单包发布台 JS 不超过 250 行
- [ ] 无新增 console.error 或 JavaScript 运行时错误
- [ ] 无硬编码绝对路径暴露（除必要的素材路径）

---

## 附：已有代码质量评估

### 优点

1. **单包发布台代码量极小**：HTML 86 行 + JS 145 行 + CSS 242 行，比综合工作台小一个数量级，易于维护
2. **结构清晰**：三栏布局（准备区/预览区/复制区）+ 底部次级工作区，符合操作顺序
3. **安全边界明确**：`manual_publish_only: true`，只打开页面不执行上传
4. **标题规则已实现**：构建时校验 + 页面端提示
5. **无视频门禁已实现**：MP4 不存在时按钮禁用
6. **验证脚本通过**：5 项检查全部通过
7. **CSS 变量体系**：使用 `--paper`、`--accent` 等语义化变量，方便换肤
8. **安全区可视化**：底部安全区开关 + 红色遮罩层，直观展示屏蔽区域

### 缺点

1. **发布按钮不检查必查清单**：P0 安全问题
2. **综合工作台过大**：2832 行 JS + 2085 行 CSS，单一职责原则被违反
3. **状态源不统一**：localStorage、JSON 文件、页面内嵌模型三套状态
4. **复制无版本提示**：可能复制旧文案
5. **验证体系分裂**：`verify_manual_publish_packages.py` 面向旧包格式，`verify_videohao_publish_desk.py` 面向新发布台
6. **数据回流入口分散**：综合工作台录入、metrics_inbox 目录、手动截图三种方式并存
7. **无 FFprobe 视频质量检查**：只检查文件存在，不检查编码/分辨率/时长
