# 视频号发布台 MVP 审核报告

> 审核人: Hermes (Agnes-2.0-Flash)
> 审核日期: 2026-06-14
> 审核范围: 单包发布台 + 综合工作台 + 构建脚本 + 门禁规则
> 项目根目录: E:\CodexProjects\videohao-content-factory

---

## 【总体结论】

**当前 MVP 可以交付日常使用，但必须修 3 个 P0 问题后才能正式投入每晚 21:00 后的疲劳操作场景。**

单包发布台的核心目标——"一条内容从准备到人工发布的关键信息集中在一个页面"——已经实现。CSS/JS 结构干净，复制功能可用，无 MP4 门禁生效，标题规则已校验。但三个问题会影响日常使用：① 综合工作台与单包发布台入口重叠导致困惑；② 发布门禁不完整（清单未全勾就能打开视频号）；③ 数据回流断点（localStorage 不写回文件）。

---

## 【评分】

| 维度 | 评分 | 说明 |
|------|------|------|
| 产品可用性 | 72/100 | 单包发布台聚焦，核心功能到位；综合工作台过载 |
| 用户操作成本 | 65/100 | 复制方便，但清单不强制、状态分散、两个入口易混淆 |
| 代码可维护性 | 60/100 | `app.js` 2700 行单文件；构建脚本重复；验证体系分裂 |
| 安全与发布边界 | 75/100 | 无自动发布、无 MP4 门禁、标题规则正确；但素材授权无强制门禁 |
| 数据闭环 | 45/100 | 观察池和学习脚本存在，但 localStorage 不写回、雷达映射为空 |

---

## 【最严重的 5 个问题】

### 问题 1：两个入口定位重叠，用户不知道该打开哪个

**影响：** 综合工作台（`preview/index.html`）有 14 个导航模块，单包发布台（`preview/videohao_publish_desk_latest.html`）聚焦一条内容。用户晚上 21:00 打开时，如果误开综合工作台，14 个选项会增加认知负担，偏离"今晚只发这一条"的目标。

**推荐改法：** 明确单包发布台为唯一日常入口。综合工作台降级为"周复盘/月分析"管理入口。启动脚本 `open_videohao_publish_desk.ps1` 只打开单包发布台，不在默认路径暴露综合工作台。

**验收方式：** 用户打开 `open_videohao_publish_desk.ps1` 后直接看到单包发布台，综合工作台需手动输入 URL 访问。

---

### 问题 2：发布门禁不强制完成全部必查项

**影响：** 当前只要有 MP4 存在，"打开视频号"按钮就启用。用户即使没有勾选"已完整预览""标题正文已核对""字幕未进入底部安全区"，也能直接跳转。这意味着疲劳状态下可能跳过关键检查。

**推荐改法：** 在 `publish_desk.js` 中，只有当所有 `required: true` 的 checklist 项都被勾选时，才启用"打开视频号"按钮。同时保留一个"强制跳过（需填写原因）"的小按钮供紧急场景使用。

**验收方式：** 无 MP4 时按钮禁用；有 MP4 但未勾选全部必查项时按钮禁用且显示"请完成全部必查项后再打开视频号"；勾选全部后按钮启用。

---

### 问题 3：数据回流断点——localStorage 不写回文件

**影响：** 综合工作台的审核状态、备注、数据录入全部保存在浏览器 `localStorage`，刷新或换浏览器就丢失。Python 侧的 `import_videohao_metrics.py` 和 `nightly_videohao_metrics_review.py` 需要手动运行，不是"一个按钮完成"的流程。

**推荐改法（MVP 阶段最小改动）：** 在单包发布台增加一个"录入发布后数据"的简单表单（播放量、点赞、评论、转发），提交后通过 `fetch()` 写到一个本地 JSON 文件（如 `data/publish_log/latest.json`）。Python 侧的 nightly 脚本读取这个文件即可。

**验收方式：** 用户在发布台录入数据后，`data/publish_log/latest.json` 中出现对应条目；nightly 脚本能读取到该数据。

---

### 问题 4：素材授权和 BGM 版权无强制门禁

**影响：** 提示词中要求无水印，但实际素材（图片、音乐）的来源和授权状态完全依赖人工判断。如果用了未授权素材发布，法律风险由用户承担。

**推荐改法：** 在发布包的 JSON 模型中增加 `assets` 数组，每个素材包含 `source`（来源）、`license`（授权类型：自制/CC0/已购买/待确认）、`confirmed`（是否已确认）。构建脚本检查 `confirmed: false` 的素材时，在发布台标记红色警告。

**验收方式：** 发布台在"素材和成品路径"下方显示"素材授权状态"区块，任何 `confirmed: false` 的素材标红并提示"未确认授权，发布前需人工核实"。

---

### 问题 5：复制提示缺少版本信息，可能复制旧文案

**影响：** 用户点击复制后只显示"已复制"，不知道复制的是哪个版本的文案。如果之前构建过多次，可能误复制了旧包内容。

**推荐改法：** 在 toast 提示中显示内容 ID、包日期和当前状态。例如："已复制标题「别把好心给错人」· 包 2026-06-09 · 状态：尚未完成"。

**验收方式：** 点击任意复制按钮后，toast 显示内容 ID、包日期和状态。

---

## 【必须立刻修的内容】（P0）

| # | 问题 | 修改范围 | 验收信号 |
|---|------|----------|----------|
| 1 | 门禁不强制必查项 | `publish_desk.js` | 必查项未全勾时"打开视频号"禁用 |
| 2 | 复制提示无版本信息 | `publish_desk.js` (showToast) | toast 显示内容 ID + 包日期 + 状态 |
| 3 | 两个入口重叠 | `open_videohao_publish_desk.ps1` + 文档 | 默认只打开单包发布台 |
| 4 | 数据不回写文件 | 新增 `data/publish_log/` + 发布台表单 | 发布后数据写入 JSON 文件 |

---

## 【可以后面再做的内容】（P1/P2）

| # | 内容 | 优先级 | 说明 |
|---|------|--------|------|
| 1 | `app.js` 拆分为多模块 | P1 | 2700 行单文件，按业务模块拆分 |
| 2 | 通用构建器替代专用脚本 | P1 | 合并 `build_*_pre_publish_prep.py` |
| 3 | MP4 编码/尺寸/水印自动检测 | P2 | 需要 ffmpeg + 视觉模型，MVP 阶段保留人工目检 |
| 4 | Git 版本控制 | P1 | 当前项目不是 Git 仓库 |
| 5 | 验证体系统一 | P1 | `verify_manual_publish_packages.py` 与 `verify_videohao_publish_desk.py` 合并 |
| 6 | 雷达→发布→观察闭环 | P2 | `radar_publish_bridge.json` mappings 为空 |
| 7 | 路径改为相对路径 | P2 | 当前硬编码 `E:\CodexProjects\...` |
| 8 | 产物归档策略 | P2 | `work/` 目录增长过快 |

---

## 【建议的页面结构】

按照用户实际操作顺序排列：

```
┌─────────────────────────────────────────────────────┐
│  顶部栏：品牌 · 当前选题 · 状态标签 · 复制全部 · 打开视频号 │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 发布准备  │  │   成品确认    │  │  发布内容     │  │
│  │          │  │              │  │              │  │
│  │ 下一步动作│  │ 视频预览      │  │ 标题 [复制]  │  │
│  │          │  │ 安全区开关    │  │ 正文 [复制]  │  │
│  │ 必查清单  │  │ 抽帧预览入口  │  │ 话题 [复制]  │  │
│  │          │  │              │  │ 置顶评论[复制]│  │
│  │ 素材路径  │  │              │  │              │  │
│  │ (可展开)  │  │              │  │ [复制整段文案]│  │
│  │          │  │              │  │              │  │
│  │ 授权状态  │  │              │  │ 人工发布提醒  │  │
│  │ (新增)    │  │              │  │              │  │
│  └──────────┘  └──────────────┘  └──────────────┘  │
│                                                     │
├─────────────────────────────────────────────────────┤
│  次级标签页：[分镜] [素材] [提示词] [数据复盘] [录入数据] │
├─────────────────────────────────────────────────────┤
│  底部：发布时间记录 · 人工发布确认 · 免责声明             │
└─────────────────────────────────────────────────────┘
```

**关键变化：**
1. 新增"素材授权状态"区块在左侧面板
2. 新增"录入数据"标签页在次级工作区
3. 新增底部"人工发布确认"时间戳记录
4. 移除综合工作台的所有 14 个导航模块

---

## 【建议新增按钮】

| 按钮名称 | 位置 | 点击后行为 |
|----------|------|------------|
| 复制全部 | 顶部栏（已有） | 复制标题+正文+话题+置顶评论 |
| 打开视频号 | 顶部栏（已有） | 无 MP4 时禁用；必查项未全勾时禁用 |
| 显示安全区 | 成品确认区（已有） | 切换底部 26% 安全区高亮 |
| 查看抽帧拼图 | 成品确认区（已有） | 弹窗展示抽帧预览大图 |
| 复制标题 | 发布内容区（已有） | 复制标题到剪贴板 |
| 复制正文 | 发布内容区（已有） | 复制正文到剪贴板 |
| 复制话题 | 发布内容区（已有） | 复制话题标签到剪贴板 |
| 复制置顶评论 | 发布内容区（已有） | 复制置顶评论到剪贴板 |
| 复制整段文案 | 发布内容区（已有） | 拼接标题+正文+话题+评论复制到剪贴板 |
| 复制路径 | 素材路径区（已有） | 复制对应路径 |
| **[新增] 录入发布数据** | 次级标签页 | 弹出表单：播放量/点赞/评论/转发/发布时间 |
| **[新增] 确认已发布** | 底部栏 | 记录发布时间戳，状态变更为"已发布" |
| **[新增] 标记素材已授权** | 素材路径区 | 点击后标记该素材为已确认授权 |

---

## 【发布前门禁规则】（MVP 阶段）

以下任一条件不满足时，禁止进入"可发布"状态：

| # | 规则 | 级别 | 检测方式 |
|---|------|------|----------|
| 1 | MP4 文件存在且非空 | P0 | `Path(video_path).exists() and Path(video_path).stat().st_size > 0` |
| 2 | 标题 ≤ 16 字，无空格，无标点 | P0 | `validate_videohao_title()` |
| 3 | 所有 `required: true` 的必查项已勾选 | P0 | 前端 checklist 状态 |
| 4 | 正文不为空 | P0 | 构建脚本校验 |
| 5 | 至少 1 张图片素材 | P0 | `detect_assets()` 检查图片目录 |
| 6 | 分镜脚本不为空 | P1 | 构建脚本校验 |
| 7 | 素材授权已确认或标红警告 | P1 | `assets[].confirmed` 字段 |
| 8 | 无高风险审核规则命中 | P1 | `audit_rules.json` 关键词扫描 |

**以下规则 MVP 阶段保留人工目检，不做自动拦截：**
- MP4 编码/尺寸/时长（需 ffmpeg）
- 画面水印/Logo 检测（需视觉模型）
- 字幕安全区合规（已有安全区可视化）
- 素材审美质量（主观判断）

---

## 【数据回流设计】

### 最简单的录入方式

在单包发布台新增一个"录入数据"标签页，包含：

```
┌──────────────────────────────────┐
│  📊 发布数据录入                  │
├──────────────────────────────────┤
│  发布时间：[2026-06-14 21:30]     │
│                                  │
│  2小时数据：                       │
│  播放量：[____]   点赞：[____]     │
│  评论：[____]     转发：[____]     │
│                                  │
│  24小时数据：                      │
│  播放量：[____]   点赞：[____]     │
│  评论：[____]     转发：[____]     │
│  新增关注：[____]                  │
│                                  │
│  [保存数据] [取消]                │
└──────────────────────────────────┘
```

**保存行为：**
- 点击"保存数据"后，写入 `data/publish_log/latest.json`
- 文件格式：
```json
{
  "content_id": "BIEBA-HAOXIN-GEICUOREN-20260609",
  "package_id": "2026-06-09_bieba_haoxin_geicuoren",
  "publish_time": "2026-06-14T21:30:00",
  "metrics_2h": {"views": 120, "likes": 15, "comments": 3, "shares": 1},
  "metrics_24h": {"views": 850, "likes": 92, "comments": 18, "shares": 7, "followers": 5},
  "notes": ""
}
```
- 同时通过 `fetch()` 调用本地 Python 端点（或写文件）持久化

**夜间学习：**
- `nightly_videohao_metrics_review.py` 读取 `data/publish_log/` 下所有 JSON
- 计算各指标均值、分位数
- 生成"可放大/需优化/止损"建议
- 写入 `data/nightly_learning_latest.json`

---

## 【给 Codex 的修改任务清单】

### 1. 立即修复（P0）

#### 任务 1.1：门禁强制必查项
- **目标：** 所有 `required: true` 的 checklist 项未全勾时，禁用"打开视频号"按钮
- **修改范围：** `preview/publish_desk_assets/publish_desk.js`
- **不允许：** 修改 HTML 结构、修改 CSS、修改 Python 侧
- **验收：** 
  - 无 MP4 时按钮禁用（已有）
  - 有 MP4 但必查项未全勾时按钮禁用
  - 勾选全部必查项后按钮启用
  - 按钮 tooltip 显示"请完成全部必查项"

#### 任务 1.2：复制提示显示版本信息
- **目标：** toast 提示显示内容 ID、包日期、状态
- **修改范围：** `preview/publish_desk_assets/publish_desk.js` 的 `showToast` 函数
- **不允许：** 修改其他交互逻辑
- **验收：** 点击任意复制按钮后，toast 显示类似"已复制标题「别把好心给错人」· 包 2026-06-09 · 状态：尚未完成"

#### 任务 1.3：启动脚本只打开单包发布台
- **目标：** `open_videohao_publish_desk.ps1` 默认打开单包发布台，不打开综合工作台
- **修改范围：** `scripts/open_videohao_publish_desk.ps1`
- **不允许：** 删除综合工作台文件
- **验收：** 运行脚本后浏览器打开 `videohao_publish_desk_latest.html`

#### 任务 1.4：新增发布数据录入标签页
- **目标：** 在次级工作区新增"录入数据"标签页，包含表单和保存按钮
- **修改范围：** 
  - `preview/videohao_publish_desk_latest.html`（新增 tab 按钮和容器）
  - `preview/publish_desk_assets/publish_desk.js`（新增渲染逻辑和保存逻辑）
  - 新增 `data/publish_log/` 目录
- **不允许：** 修改现有标签页、修改 CSS（复用现有样式）
- **验收：** 
  - 点击"录入数据"标签页显示表单
  - 填写后点击保存，写入 `data/publish_log/latest.json`
  - 文件包含 content_id、publish_time、metrics_2h、metrics_24h

### 2. 体验优化（P1）

#### 任务 2.1：新增素材授权状态区块
- **目标：** 在左侧面板显示素材授权状态
- **修改范围：** 
  - Python 构建脚本增加 `assets` 数组到模型
  - 发布台 JS 渲染授权状态
  - 可选：发布台增加"标记已授权"按钮
- **验收：** 任何 `confirmed: false` 的素材标红显示

#### 任务 2.2：新增"确认已发布"按钮
- **目标：** 底部栏记录发布时间戳
- **修改范围：** 发布台 HTML + JS
- **验收：** 点击后写入 `data/publish_log/latest.json` 的 `published_at` 字段

### 3. 安全门禁（P1）

#### 任务 3.1：综合工作台门禁与单包发布台对齐
- **目标：** 确保 `app.js` 中的 `gateDecision()` 逻辑与单包发布台一致
- **修改范围：** `preview/app.js`、`scripts/videohao_publish_desk_renderer.py`
- **验收：** 两者对同一内容的门禁结果相同

#### 任务 3.2：标题规则页面端拦截增强
- **目标：** 标题不合规时，在复制按钮旁显示红色警告图标
- **修改范围：** `preview/publish_desk_assets/publish_desk.js`
- **验收：** 标题违规时，标题卡片右上角显示 ⚠️

### 4. 数据回流（P1）

#### 任务 4.1：nightly 脚本读取 publish_log
- **目标：** `nightly_videohao_metrics_review.py` 读取 `data/publish_log/` 下的 JSON 文件
- **修改范围：** `scripts/nightly_videohao_metrics_review.py`
- **验收：** 能从 publish_log 读取数据并参与学习分析

#### 任务 4.2：雷达桥接初始化
- **目标：** `radar_publish_bridge.json` 增加基础结构，等待真实映射
- **修改范围：** `data/radar_publish_bridge.json`
- **验收：** 文件有正确的 schema，`mappings` 初始化为空数组而非 null

### 5. 验收标准

| # | 标准 | 验证方式 |
|---|------|----------|
| 1 | 能在 3 分钟内完成一条视频的发布前检查 | 计时操作：打开页面→看状态→核对清单→复制文案→关闭 |
| 2 | 所有标题自动检测 16 字以内无标点 | `validate_videohao_title()` 对违规标题返回错误 |
| 3 | 所有关键文案都有一键复制 | 标题、正文、话题、置顶评论、整段文案均有复制按钮 |
| 4 | 无 MP4 时"打开视频号"禁用 | 页面加载后按钮 disabled |
| 5 | 必查项未全勾时"打开视频号"禁用 | 勾选部分必查项后按钮仍 disabled |
| 6 | 复制提示显示版本信息 | toast 包含内容 ID、包日期、状态 |
| 7 | 水印素材不能通过门禁 | 素材授权 `confirmed: false` 时标红警告 |
| 8 | 未审核内容不能显示为可发布 | `status.code` 非发布态时，页面显示对应状态 |
| 9 | 发布后数据能录入并进入观察池 | 表单保存后 `data/publish_log/latest.json` 有数据 |
| 10 | nightly 脚本能读取发布数据 | 运行脚本后输出包含 publish_log 数据 |
| 11 | 验证脚本全部通过 | `python scripts/verify_videohao_publish_desk.py` 输出 `{"no_video_state": "ok"}` |
| 12 | 启动脚本只打开单包发布台 | 运行 `open_videohao_publish_desk.ps1` 后浏览器打开正确页面 |

---

## 【风险问题清单】

| 风险 | 严重程度 | 当前状态 | 缓解措施 |
|------|----------|----------|----------|
| 错发旧版本 | 高 | 无版本校验 | 任务 1.2 复制提示显示包日期 |
| 无 MP4 或坏 MP4 | 高 | 仅检查文件存在 | P2 增加 ffmpeg 检测 |
| 标题违规 | 中 | 构建时校验，页面仅提示 | 任务 3.2 增强页面端警告 |
| 素材版权和水印 | 高 | 提示词约束，无强制门禁 | 任务 2.1 新增授权状态 |
| 数据误归属 | 中 | 无内容 ID 关联 | 任务 1.4 录入数据时自动关联 content_id |
| 本地状态与文件状态不一致 | 高 | localStorage 不写回 | 任务 1.4 写入 JSON 文件 |
| 样本少导致错误学习 | 中 | 已标注"早期样本" | 任务 4.2 初始化雷达桥接 |
| 疲劳状态下误操作 | 高 | 清单不强制 | 任务 1.1 强制必查项 |

---

## 【最终一句话总结】

MVP 核心目标已达成——单条内容的发布信息集中到一个页面，无 MP4 时门禁生效，复制功能可用。下一阶段的重点不是加功能，而是**修门禁、统一入口、打通数据回流**。建议先完成 4 个 P0 任务（强制必查项、版本提示、入口收敛、数据录入），再进行日常使用。
