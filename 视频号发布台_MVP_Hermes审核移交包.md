# 视频号发布台 MVP 审核请求

> 交接类型：高级架构与产品审核前移交包  
> 扫描日期：2026-06-14  
> 真实项目根目录：`E:\CodexProjects\videohao-content-factory`  
> 本文依据：本地代码、JSON 数据、发布包、验证脚本与已有审计报告的只读扫描  
> 当前最新发布包：`2026-06-09_bieba_haoxin_geicuoren`  
> 当前最新发布台状态：`local_images_ready_pending_render`，已有 5 张图片和音乐，尚无成品 MP4，不能视为可发布状态

Hermes，你现在作为：

- 产品架构审核负责人
- 用户体验负责人
- 代码质量审核负责人
- 增长系统设计顾问

请审核 Codex 完成的《视频号发布台 MVP》。

请注意：项目里同时存在两层页面，审核时不要混为一谈。

1. **单包视频号发布台 MVP**：`preview/videohao_publish_desk_latest.html`。目标是让用户针对当天一条视频完成成片预览、文案复制、路径确认和人工发布。
2. **微信视频号增长工厂 V2.0 综合工作台**：`preview/index.html + preview/app.js + preview/styles.css`。包含内容状态、素材缺口、门禁、观察池、数据录入、规则、学习、圆桌和雷达等模块，但主要是静态前端读取 JSON，部分操作只保存在浏览器 `localStorage`。

---

## 一、项目背景

这是《微信视频号增长工厂 V2.0》的发布辅助系统。

核心目标是帮助用户每天快速完成：

```text
选题
↓
素材检查
↓
文案确认
↓
发布准备
↓
人工发布
↓
数据回流
↓
系统学习
```

系统不负责自动发布。

明确边界：

- 禁止自动发布微信视频号。
- 禁止抓取微信后台私有数据。
- 禁止绕过登录、验证码、平台限制或风控。
- 禁止发布未经人工审核的内容。
- 禁止使用来源不明、版权不清或带水印的素材。
- 微信视频号后台的最终上传和“发布”按钮必须由用户手动完成。
- 发布后数据主要来自用户提供的截图、CSV 或 JSON，不得声称来自微信 API。

### 当前真实状态

- 发布台已经具备单条内容的发布准备页面和生成器。
- 最新发布台能够正确阻止“没有 MP4 的内容”进入人工发布入口。
- 最新包已有标题、正文、话题、置顶评论、分镜、提示词、5 张图片和候选音乐。
- 最新包尚未生成成品 MP4，因此页面显示“尚未完成”，视频号发布按钮禁用。
- 数据回流和学习链路存在，但样本仍少，且字段、入口和状态来源尚未完全统一。

---

## 二、为什么开发这个 MVP

每天做视频号时，用户需要在多个文件、页面和工具之间寻找：

- 今天发什么选题。
- 最终标题和封面文字。
- 正文、话题和置顶评论。
- 使用哪些图片、音乐和成品视频。
- 分镜与生成提示词。
- 标题是否超长或带标点。
- 素材是否重复、低清、带文字、带 Logo、带水印或来源不明。
- 视频是否为正确版本、正确尺寸、正确编码。
- 发布前还有哪些步骤没完成。
- 发布后应记录哪些数据。

完全人工操作容易出现：

- 漏步骤。
- 打开或发送旧版本。
- 找不到最终文案。
- 忘记检查标题、素材、水印、字幕安全区和成片结尾。
- 没有 MP4 时仍误以为内容可发布。
- 发布后忘记记录 2h、24h、7d 数据。
- 数据散落，无法回流到下一次选题和脚本决策。

因此开发发布台，目标不是增加更多功能，而是把“一条内容从准备到人工发布”的关键材料集中在一个本地页面，并把发布门禁和下一步动作显式展示。

---

## 三、当前 MVP 已实现功能

以下内容均根据真实代码和数据列出。未实现或未验证的部分会明确标注。

### 功能 1：单条发布包生成

**名称：** 当日视频号预发布包  
**作用：** 把标题、正文、话题、置顶评论、分镜、图片提示词、视频提示词、素材路径和状态整理到固定目录。  
**入口：** 当前最新入口为 `scripts/build_bieba_haoxin_geicuoren_pre_publish_prep.py`。其他主题有各自的 `build_*_pre_publish_prep.py`。  
**涉及文件：**

- `publish_packages/pre_publish_prep/2026-06-09_bieba_haoxin_geicuoren/`
- `data/bieba_haoxin_geicuoren_pre_publish_prep_latest.json`
- `reports/bieba_haoxin_geicuoren_pre_publish_prep_latest.md`
- `preview/bieba_haoxin_geicuoren_pre_publish_prep_latest.html`
- `preview/videohao_publish_desk_latest.html`

**核心函数：**

- `detect_assets()`：识别图片、MP4、联系表和音乐。
- `upload_copy()`：生成发布文案。
- `render_image_prompts()`：生成图片提示词文档。
- `render_shot_script()`：生成分镜文档。
- `render_preview()`：构建发布台页面模型并渲染 HTML。
- `main()`：校验标题、创建目录、检测状态、写出各类产物。

**当前完成度：** 可用，但不同主题仍存在多个专用构建脚本。  
**限制：** 最新包没有 MP4；发布包生成器与视频渲染不是统一入口。

### 功能 2：单包发布台页面

**名称：** 视频号发布台  
**作用：** 让用户在一个本地页面查看发布状态、预览成片、确认文案、复制路径并手动打开视频号发布页。  
**入口：**

- 页面：`preview/videohao_publish_desk_latest.html`
- 启动脚本：`scripts/open_videohao_publish_desk.ps1`

**涉及文件：**

- `scripts/videohao_publish_desk_renderer.py`
- `preview/publish_desk_assets/publish_desk.js`
- `preview/publish_desk_assets/publish_desk.css`

**核心能力：**

- 展示当前主题、日期、状态和下一步动作。
- 展示必查清单，勾选结果保存在 `localStorage`。
- 有 MP4 时显示视频预览；无 MP4 时显示空状态。
- 显示底部安全区开关。
- 显示抽帧联系表入口，前提是存在联系表。
- 复制标题、正文、话题、置顶评论和整段发布文案。
- 复制发布包、图片目录、视频目录、成品视频、音乐等路径。
- 显示分镜、素材、提示词、数据复盘四个次级标签页。
- 只有 `status.video_ready=true` 时才允许打开视频号发布页。

**当前完成度：** 页面和交互可用，专用验证脚本通过。  
**限制：**

- 清单勾选只保存在当前浏览器本地，不会写回发布包。
- “打开视频号”仅打开固定网页，不自动上传或发布。
- 页面模型内嵌在 HTML 中，重新构建后会覆盖。

### 功能 3：发布状态与无视频门禁

**名称：** 发布就绪门禁  
**作用：** 没有真实存在的 MP4 时，不把内容标记为可发布，并禁用视频号入口。  
**入口：** `scripts/videohao_publish_desk_renderer.py` 的 `make_publish_model()`。  
**真实逻辑：**

- `video_ready = bool(video_path and Path(video_path).exists())`
- 有视频时状态显示“可手动发布”。
- 无视频时状态显示“尚未完成”。
- 页面 JS 根据 `status.video_ready` 决定是否启用“打开视频号”。

**当前完成度：** 已实现并通过 `verify_videohao_publish_desk.py` 的无视频状态验证。  
**限制：** 该门禁只判断文件是否存在，不在发布台生成阶段再次检查视频编码、尺寸、时长、音频、黑帧或水印。

### 功能 4：标题硬规则

**名称：** 视频号标题规则  
**作用：** 校验标题不为空、不超 16 字、不含空格和标点。  
**入口：** `scripts/videohao_publish_rules.py`。  
**核心函数：**

- `validate_videohao_title(title)`
- `assert_videohao_title(title)`

**当前完成度：** 构建最新发布包时调用；页面端也显示标题字数并标记无效状态。  
**限制：** 页面端只提示，不会阻止复制；综合工作台还存在另一套审核规则计算逻辑。

### 功能 5：发布文案集中复制

**名称：** 发布内容复制  
**作用：** 减少用户在文件夹中寻找标题、正文、话题和置顶评论。  
**入口：** 单包发布台右侧“逐项确认并复制”。  
**涉及文件：** `preview/publish_desk_assets/publish_desk.js`。  
**真实交互：**

- 使用 `navigator.clipboard.writeText()`。
- 失败时回退到隐藏文本框和 `document.execCommand("copy")`。
- 支持逐项复制、整段复制和路径复制。

**当前完成度：** 已实现。  
**限制：** 没有复制后内容校验、版本号或“最后复制的是哪个版本”提示。

### 功能 6：素材与提示词可追溯

**名称：** 素材路径与提示词整理  
**作用：** 保留图片目录、视频目录、音乐、分镜、图片提示词和视频提示词。  
**入口：** 发布包目录和单包发布台次级标签页。  
**当前完成度：** 已实现。  
**限制：** 素材授权信息并未在最新单包页面里形成逐素材强制门禁；最新包的音乐文件名为候选音乐，版权状态需要人工确认。

### 功能 7：安全区与无文字提示词约束

**名称：** 视频号画面安全约束  
**作用：** 避免字幕被底部 UI 遮挡，避免 AI 图片生成乱码文字、Logo 和水印。  
**真实实现：**

- 分镜文档要求顶部字幕、底部至少 500px 安全区。
- 最新提示词要求画面底部 35% 留空。
- 图片和视频提示词明确禁止文字、字母、数字、标牌、Logo、水印。
- 发布台可切换显示底部安全区。

**当前完成度：** 约束和可视提示已存在。  
**限制：** 当前没有自动视觉检测证明最终 MP4 确实无水印、无乱码文字且安全区合规。

### 功能 8：综合工作台发布前门禁

**名称：** 增长工厂 V2.0 发布前门禁  
**作用：** 把规则风险、素材缺口、发布包验证和发布状态放在同一页面判断。  
**入口：** `preview/index.html` 的“发布前门禁”。  
**涉及文件：** `preview/app.js`。  
**核心函数：**

- `auditReviewFor(contentId)`：根据 `data/audit_rules.json` 扫描标题、脚本等文本风险。
- `gateDecision(row)`：根据素材缺口、规则风险和发布包验证给出门禁判断。
- `renderPublishGateBoard()`：渲染门禁页面。
- `setLocalStatus(contentId, status)`：高风险时阻止切换到已通过或已排期。

**当前完成度：** 已实现前端判断。  
**限制：** 状态更新主要写入浏览器 `localStorage`，不是服务端或文件级权威状态；规则属于关键词筛查，不能替代平台审核。

### 功能 9：发布后数据录入和观察池

**名称：** 2h / 24h / 7d 数据回流  
**作用：** 提醒用户发布后录入数据，并建立后续复盘入口。  
**入口：**

- 综合工作台“发布后观察池”
- 综合工作台“发布数据录入”
- `scripts/import_videohao_metrics.py`
- `scripts/ingest_inbox_metrics.py`
- `scripts/nightly_videohao_metrics_review.py`

**核心函数：**

- `pendingMetricNodes()`：计算待录入节点。
- `startMetricEntryFromObservation()`：从观察池进入指定内容和时间节点的数据录入。
- `persistMetricNode()`：保存页面本地数据。
- `normalize_row()` / `import_metrics()`：导入 CSV/JSON 指标。
- `load_publish_metrics()` / `recommend_next()`：读取真实指标并生成下一步建议。

**当前完成度：** 页面、数据文件和脚本均存在。  
**限制：**

- 页面直接录入的数据保存在 `localStorage`，不会自动写入项目 JSON。
- 文件导入需要单独运行 Python 脚本。
- 当前真实样本较少，学习结论只能视为早期规则判断。

### 功能 10：增长学习与下一条建议

**名称：** 数据复盘与内容方向建议  
**作用：** 根据本地发布指标判断“可放大”或“读者不买账”，并生成下一条内容建议。  
**入口：**

- `data/nightly_videohao_metrics_review_latest.json`
- `reports/nightly_videohao_metrics_review_latest.md`
- `scripts/nightly_videohao_metrics_review.py`
- 综合工作台“爆点学习”

**当前完成度：** 已存在并使用真实截图录入数据。  
**限制：** 样本数量少；部分分类、阈值和推荐语句为代码内规则，不是训练模型。

### 功能 11：热点雷达到发布队列桥接

**名称：** 雷达选题桥接  
**作用：** 将热点候选映射到发布队列，并为后续观察池回流预留关联。  
**入口：**

- `scripts/import_radar_items.py`
- `scripts/build_daily_radar_brief.py`
- `scripts/bridge_radar_to_publish_queue.py`
- `data/radar_items.json`
- `data/radar_publish_bridge.json`

**当前完成度：** 雷达页面和脚本存在。  
**限制：** `data/radar_publish_bridge.json` 当前 `mappings` 为空，说明雷达到发布、再到数据回流的真实闭环尚未建立。

---

## 四、当前页面结构

### A. 单包视频号发布台

用户打开 `preview/videohao_publish_desk_latest.html` 后看到：

**顶部区域**

- “视频号发布台 / 手动发布工作台”标识。
- 当前主题和封面文字。
- 当前状态标签。
- “复制全部”按钮。
- “打开视频号”按钮；没有 MP4 时禁用。

**左侧发布准备区**

- 状态标题。
- 当前下一步动作。
- 发布前必查清单。
- 标题最多 16 字、无标点空格、仅人工发布的门禁说明。
- 可展开的素材和成品路径。

**中间成品确认区**

- 竖屏视频预览。
- 视频未生成时的明确空状态。
- 安全区显示开关。
- 联系表/抽帧预览入口；仅存在联系表时显示。

**右侧发布内容区**

- 标题字数计数。
- 标题、正文、话题、置顶评论逐项展示和复制。
- 整段发布文案复制。
- “仍需手动上传并点击发布”的提醒。

**底部次级工作区**

- 分镜。
- 素材路径。
- 图片/视频提示词。
- 人工导入的数据复盘。

### B. 增长工厂 V2.0 综合工作台

用户打开 `preview/index.html` 后看到：

**左侧导航**

- 增长首页。
- 内容状态中心。
- 素材缺口中心。
- 发布前门禁。
- 发布后观察池。
- 发布数据录入。
- 内容审核。
- 规则检查。
- 生产流水线。
- 爆款素材库。
- AI 选题引擎。
- 爆点学习。
- AI 专家圆桌。
- 热点情报雷达。

**左侧统计与筛选**

- 待审核、今日待发布、高风险、粉丝等统计。
- 标题/分类/标签搜索。
- 分类、风险、状态筛选。
- 30s / 60s / 90s 脚本版本切换。
- 导出队列和重置本页状态。

**主要内容区**

- 各模块使用 JS 动态读取 `data/*.json` 渲染。
- 用户在页面中的审核状态、备注和部分数据录入保存于 `localStorage`。

### 页面复杂度判断

单包发布台比较聚焦，符合“晚上打开后完成一条发布准备”的目标。综合工作台功能明显更多，已超过单纯 MVP 发布台范围，普通用户可能难以判断每天应该先打开哪个入口。

---

## 五、完整用户流程

### 理想人工发布流程

```text
用户晚上 21 点打开视频号发布台
↓
查看当前主题、状态和“下一步动作”
↓
确认标题、正文、话题、置顶评论
↓
确认图片、音乐、视频路径和素材来源
↓
如果尚无 MP4：停止发布，先完成视频合成
↓
如果已有 MP4：完整播放成片
↓
打开安全区显示，检查字幕和关键人物没有被底部 UI 遮挡
↓
检查画面是否有乱码文字、Logo、水印、低清、重复画面
↓
勾选“已完整预览”“标题正文已核对”等必查项
↓
复制标题、正文、话题、置顶评论
↓
点击“打开视频号”
↓
用户在微信视频号后台手动上传 MP4
↓
用户人工粘贴并最终检查发布文案
↓
用户手动点击发布
```

### 发布后数据回流流程

```text
发布完成
↓
登记发布时间和内容 ID
↓
到 2h / 24h / 7d 节点
↓
用户从微信视频号后台截图、导出或手工记录数据
↓
通过综合工作台录入，或放入 data/imports / metrics_inbox 后运行导入脚本
↓
生成 publish_metrics_*.json / videohao_published_metrics.json
↓
nightly_videohao_metrics_review.py 汇总真实表现
↓
增长驾驶舱与学习系统给出保留、放大、止损和下一条建议
↓
建议进入下一轮选题和发布包
```

### 当前流程断点

1. 最新单包尚未生成 MP4，流程停在“素材到位，等待合成”。
2. 页面清单和录入状态主要在浏览器 `localStorage`，不会自动写回项目文件。
3. “人工发布完成”没有统一、强制的登记入口。
4. 雷达到发布队列的映射文件当前为空。
5. 数据回流到学习系统存在脚本，但不是一个按钮完成的统一流程。

---

## 六、当前技术结构

### 前端

没有 React、Vue 等框架。

- 单包发布台：静态 HTML + 共享 CSS + 原生 JavaScript。
- 综合工作台：`preview/index.html + preview/app.js + preview/styles.css`。
- 页面通过 `fetch()` 读取相对路径下的 JSON。
- 用户临时状态通过浏览器 `localStorage` 保存。
- 剪贴板复制优先使用 `navigator.clipboard`，失败时使用旧式复制回退。

### 后端

严格来说，当前发布台没有业务后端。

- 本地预览由 `python -m http.server` 提供静态文件服务。
- Python 脚本负责生成 HTML、JSON、Markdown、发布包、报告和数据复盘。
- 微信视频号后台不在系统控制范围内。

### 数据

主要数据位于 `data/`：

- 内容库与发布队列。
- 增长策略和仪表盘数据。
- 审核规则。
- 素材缺口。
- 发布包验证。
- 发布后指标。
- 学习权重和学习报告。
- 雷达选题与桥接映射。

数据是多个 JSON 文件，没有统一数据库和 schema 版本治理。

### 本地文件

主要产物：

- `publish_packages/`：发布包、素材、视频、清单、文案。
- `preview/`：发布台、综合工作台和历史预览页面。
- `reports/`：构建、渲染、审核、复盘报告。
- `rendered_videos/`：历史成片和 manifest。
- `roundtable/`：多模型回复和 Codex 裁决。
- `work/`：实验和缓存，已有大量文件。

### 自动化脚本

脚本集中于 `scripts/`，主要类型：

- `build_*`：构建发布台、发布包和增长驾驶舱。
- `render_*`：生成 MP4、抽帧、联系表和报告。
- `verify_*`：验证发布包、页面和规则。
- `import_* / ingest_* / register_*`：导入指标、登记人工发布。
- `nightly_* / learning_* / update_*`：复盘和学习。
- `radar_* / bridge_*`：热点雷达与发布队列。

### 运行方式

- 综合工作台：运行 `start_preview_server.bat`，打开 `http://127.0.0.1:8765/preview/index.html`。
- 单包发布台：运行 `scripts/open_videohao_publish_desk.ps1`，打开 `http://127.0.0.1:8765/preview/videohao_publish_desk_latest.html`。

### 测试现状

- `package.json` 的 `npm test` 仍是默认失败占位命令，没有 JS 自动测试。
- 有 Python 专用验证脚本，但没有统一总测试入口。
- 本次只读运行 `scripts/verify_videohao_publish_desk.py`，5 个检查项全部通过。
- 本次验证最新发布包 JSON 和审核规则 JSON 均可解析。

---

## 七、核心文件清单

| 文件 | 作用 | Hermes 是否需要重点看 |
| -- | -- | -- |
| `preview/videohao_publish_desk_latest.html` | 当前最新单包发布台入口；内嵌当前发布模型 | 是：确认首屏是否足够省力、状态是否清楚 |
| `preview/publish_desk_assets/publish_desk.js` | 发布台复制、清单、预览、安全区、标签页和打开视频号交互 | 是：审核门禁是否足够、是否存在误操作 |
| `preview/publish_desk_assets/publish_desk.css` | 单包发布台视觉和响应式布局 | 是：审核疲劳状态下的可读性 |
| `scripts/videohao_publish_desk_renderer.py` | 通用发布台模型和 HTML renderer | 是：当前单包发布台最核心生成模块 |
| `scripts/build_bieba_haoxin_geicuoren_pre_publish_prep.py` | 最新主题发布包生成器 | 是：审核专用脚本是否应收敛为配置驱动 |
| `scripts/videohao_publish_rules.py` | 标题 16 字、无空格、无标点校验 | 是：审核规则边界 |
| `scripts/verify_videohao_publish_desk.py` | 发布台共享资源、敏感票据和无视频状态验证 | 是：审核测试覆盖缺口 |
| `scripts/sanitize_videohao_publish_links.py` | 清理发布链接中的临时票据等风险信息 | 是：审核安全处理是否完整 |
| `scripts/open_videohao_publish_desk.ps1` | 启动本地静态服务并打开发布台 | 否：除非审核启动稳定性 |
| `data/bieba_haoxin_geicuoren_pre_publish_prep_latest.json` | 最新发布包结构化状态 | 是：审核数据模型和当前状态 |
| `publish_packages/pre_publish_prep/2026-06-09_bieba_haoxin_geicuoren/` | 最新发布包原始文档、素材与视频目录 | 是：审核发布包是否自包含 |
| `preview/index.html` | 增长工厂 V2.0 综合工作台页面结构 | 是：审核是否过度复杂 |
| `preview/app.js` | 综合工作台全部前端业务逻辑，约 2700 行 | 是：审核单文件过大、状态和职责混合 |
| `data/audit_rules.json` | 医疗、夸大、引流、权威、版权、数字、恐吓、素材质量等规则 | 是：审核规则漏项和误判风险 |
| `data/manual_publish_package_verification.json` | 旧批次发布包验证结果 | 是：审核是否能代表当前 pre_publish_prep 包 |
| `scripts/verify_manual_publish_packages.py` | 检查包、清单、指标模板、门禁、视频编码和尺寸 | 是：审核验证逻辑能否统一复用 |
| `data/videohao_published_metrics.json` | 用户截图和导入产生的真实发布指标集合 | 是：审核指标口径 |
| `scripts/import_videohao_metrics.py` | CSV/JSON 指标导入与内容匹配 | 是：审核模糊匹配和错误归属风险 |
| `scripts/nightly_videohao_metrics_review.py` | 汇总指标并给出下一条建议 | 是：审核规则判断是否被误当预测 |
| `data/nightly_videohao_metrics_review_latest.json` | 当前复盘与下一条建议 | 是：审核样本量和结论可信度 |
| `data/radar_publish_bridge.json` | 雷达到发布队列映射 | 是：当前为空，是闭环断点 |
| `reports/project_audit.md` | 已有项目结构审计 | 是：已有技术债基线 |
| `reports/publish_workflow_review.md` | 已有发布流程审计 | 是：已有人工步骤和风险判断 |
| `reports/red_team_attack.md` | 已有红队问题清单 | 是 |
| `reports/blue_team_review.md` | 已有架构扩展风险清单 | 是 |

---

## 八、当前安全设计

### 已实现

**人工发布边界**

- 单包发布模型包含 `manual_publish_only: true`。
- 发布台明确提示仍需用户手动上传并点击发布。
- 系统只打开视频号发布页，不执行上传和提交。

**无成品视频门禁**

- MP4 路径不存在时，`video_ready=false`。
- 页面显示“尚未完成”。
- “打开视频号”按钮禁用。

**标题规则**

- 标题最多 16 字。
- 禁止空格。
- 禁止标点。
- 构建脚本会调用硬校验；页面显示字数和无效状态。

**发布前风险检查**

- `data/audit_rules.json` 包含医疗误导、夸大承诺、违规引流、虚假权威、版权风险、未证实数字、过强恐吓钩子、素材质量风险。
- 综合工作台遇到高风险内容时，会阻止本地状态改成“已通过”或“已排期”。

**素材与水印约束**

- 图片和视频提示词明确禁止文字、Logo 和水印。
- 生成提示词要求保留顶部字幕空间和底部平台安全区。
- 发布台支持安全区显示。

**敏感票据清理**

- `sanitize_videohao_publish_links.py` 用于清理临时票据或不安全发布链接。
- `verify_videohao_publish_desk.py` 检查无视频页面不包含 `pass_ticket` 和 `exportkey`。

**人工确认**

- 页面提供发布前清单。
- 页面要求完整预览成片并核对标题正文。

### 当前未实现或需要 Hermes 审核

- 当前未实现最终 MP4 的自动水印、Logo、乱码文字和可读屏幕检测。
- 当前未实现最新 `pre_publish_prep` 包与旧 `manual_publish_package_verification.json` 的统一验证。
- 当前未实现清单全部勾选后才允许打开视频号；目前只要有 MP4，按钮即可启用。
- 当前未实现素材授权和 BGM 版权状态的强制字段与强制门禁。
- 当前未实现页面状态写回权威数据源。
- 当前未实现内容发布后的统一登记和防重复发布机制。
- 当前未实现对视频路径“是否为当前包最终版本”的哈希或版本校验。
- 当前未实现最终发布前的总验证命令。

---

## 九、当前不足和 Codex 自评

### 问题 1：当前最新发布包没有 MP4

**影响：** 页面虽然可用，但当前任务不能真正进入人工发布阶段。  
**建议 Hermes 判断：** MVP 验收应以“至少一个最新包可完整走通人工发布前流程”为标准，还是允许发布台先以未完成状态交付。

### 问题 2：单包发布台与综合工作台定位重叠

**影响：** 用户可能不知道每天应打开哪个页面，两个入口展示的状态也可能不一致。  
**建议 Hermes 判断：** 是否把单包发布台定为唯一日常入口，把综合工作台降为管理和复盘入口。

### 问题 3：综合工作台 `app.js` 职责过多

**影响：** 一个文件同时处理审核、队列、门禁、观察池、指标、学习、圆桌、素材提示词和雷达，维护和测试困难。  
**建议 Hermes 判断：** 下一轮是否必须按业务模块拆分，还是早期阶段继续保持单文件以降低工程复杂度。

### 问题 4：状态来源不统一

**影响：** JSON 文件、发布包、页面内嵌模型、`localStorage`、历史报告都可能表达状态，容易出现“页面已通过但文件未更新”。  
**建议 Hermes 判断：** 选定哪个文件作为发布状态权威源，并定义状态写回方式。

### 问题 5：发布门禁不要求完成全部人工清单

**影响：** 只要 MP4 存在，“打开视频号”按钮就可启用，即使用户没有勾选完整预览、标题核对等步骤。  
**建议 Hermes 判断：** 是否应该要求所有必查项勾选后再启用入口，同时保留人工绕过说明。

### 问题 6：视频文件存在不等于视频可发布

**影响：** 当前单包门禁只判断路径存在，未检查 1080x1920、H.264/AAC、时长、音轨、黑帧、结尾突兀、水印和安全区。  
**建议 Hermes 判断：** 哪些检查应成为 P0 强制门禁，哪些保留人工目检。

### 问题 7：最新包与验证体系分裂

**影响：** `verify_manual_publish_packages.py` 面向旧的 `publish_packages/2026-06-02_image_text_batch`，而最新单包位于 `publish_packages/pre_publish_prep/`。  
**建议 Hermes 判断：** 是否建立一个通用 verifier，覆盖所有当前包格式。

### 问题 8：不同主题有多个专用构建脚本

**影响：** 每条主题复制一份 `build_*_pre_publish_prep.py`，短期快，长期容易规则漂移。  
**建议 Hermes 判断：** 是否改为“通用构建器 + 内容配置 JSON”，以及何时改最划算。

### 问题 9：缺少统一测试入口

**影响：** `npm test` 是默认失败占位；验证脚本分散，开发者不知道提交前必须运行哪些检查。  
**建议 Hermes 判断：** 定义一个最小 `verify_publish_mvp` 总入口和验收清单。

### 问题 10：项目不是 Git 仓库

**影响：** 自动生成器、数据脚本和页面调整缺少可靠版本回滚与变更审查。  
**建议 Hermes 判断：** 是否在继续迭代前必须纳入 Git，或至少建立小范围备份和变更日志。

### 问题 11：真实数据样本太少

**影响：** 当前学习结论容易被少量样本误导；规则建议可能被用户误认为预测能力。  
**建议 Hermes 判断：** 页面是否必须醒目标注“早期样本，仅规则判断”，并定义多少条有效闭环后升级判断方式。

### 问题 12：指标口径和文件结构尚未完全统一

**影响：** `publish_metrics_*`、`videohao_published_metrics.json`、页面本地录入和 nightly review 之间存在字段差异。  
**建议 Hermes 判断：** 是否先冻结统一 schema，再扩展数据分析。

### 问题 13：雷达到发布到观察的桥接尚未真实使用

**影响：** `radar_publish_bridge.json` 的 `mappings` 为空，系统尚不能证明“热点来源 → 发布内容 → 实际表现”的闭环。  
**建议 Hermes 判断：** 这是否应成为下一轮唯一增长闭环重点。

### 问题 14：素材授权和音乐版权不是强制门禁

**影响：** 提示词要求无水印，但实际素材和 BGM 是否授权仍主要依赖人工判断。  
**建议 Hermes 判断：** 是否给每个素材增加来源、授权、人工确认和禁止发布状态。

### 问题 15：页面可能在疲劳状态下信息过多

**影响：** 综合工作台有 14 个主导航模块，晚上发布时会增加选择成本。  
**建议 Hermes 判断：** 是否需要“今晚只做这 3 步”的极简模式，哪些按钮和模块应隐藏。

### 问题 16：复制操作缺少版本与防错提示

**影响：** 用户可能复制了旧页面或旧包文案，但页面只提示“已复制”，没有显示包 ID、版本或更新时间。  
**建议 Hermes 判断：** 是否在复制提示中加入内容 ID、包日期和当前状态。

### 问题 17：本地静态服务器和绝对路径依赖 Windows 环境

**影响：** 项目路径、启动脚本和发布包路径大量硬编码为 `E:\CodexProjects\videohao-content-factory`，迁移成本高。  
**建议 Hermes 判断：** MVP 是否暂时接受单机绑定，还是应尽早改为相对路径和项目根自动发现。

### 问题 18：项目产物和历史实验增长过快

**影响：** `work/`、`preview/`、`scripts/`、`reports/` 中有大量实验和历史入口，扫描、维护和主链路识别越来越困难。  
**建议 Hermes 判断：** 是否建立 registry、归档策略和主链路标签，而不是继续新增页面。

---

## 十、请求 Hermes 重点审核

请 Hermes 重点判断：

1. 当前单包发布流程是否足够省力，还是仍然需要用户在太多文件和页面之间切换？
2. 普通用户晚上疲劳状态下，是否还能不出错地完成发布？
3. 单包发布台和综合工作台的定位是否清楚？综合工作台有没有复杂化？
4. 当前发布门禁是否遗漏了必须阻止发布的风险？
5. 项目是否已经过度开发？哪些模块只是“看起来完整”，但没有形成真实闭环？
6. 哪些按钮、入口、页面或标签应该删除、隐藏或降级？
7. 哪些功能必须补，才能把 MVP 称为稳定可用？
8. 是否真正形成以下闭环：

```text
内容生产
↓
发布准备
↓
人工发布
↓
反馈数据
↓
学习与下一条决策
```

9. 哪个数据文件应成为发布状态的唯一权威源？
10. 应如何在不自动发布、不抓取微信后台的前提下，让人工流程更省力？
11. 下一轮应该优先修流程、修数据、修测试还是修 UI？
12. 当前是否应该暂停所有新功能，只做主链路收敛和验收？

---

## 十一、请 Hermes 输出格式

请按以下格式返回，不要只给泛泛建议：

### 【总体评分】

0-100 分，并分别给出：

- 产品可用性评分
- 普通用户操作成本评分
- 代码可维护性评分
- 安全与发布边界评分
- 数据闭环评分

### 【是否建议继续基于当前版本迭代】

明确回答：

- 建议继续
- 建议局部重构后继续
- 建议停止当前路线并重新收敛

并说明依据。

### 【必须修改的问题】

按 P0 / P1 / P2 排序。每项说明：

- 问题
- 影响
- 推荐改法
- 验收方式

### 【建议删除的功能】

请具体到页面、按钮、模块或脚本类型，并说明删除理由。

### 【建议增加的功能】

只列真正影响发布正确性和闭环的功能，不要扩张需求。

### 【UI 重新布局建议】

请分别给出：

- 晚上人工发布的极简首屏
- 管理与复盘的次级页面
- 应隐藏或折叠的内容

### 【代码结构建议】

重点判断：

- 通用构建器与专用脚本的边界
- `preview/app.js` 是否拆分
- 状态权威源
- 数据 schema
- 验证入口
- Git 与产物归档策略

### 【风险问题】

至少覆盖：

- 错发旧版本
- 无 MP4 或坏 MP4
- 标题违规
- 素材版权和水印
- 数据误归属
- 本地状态与文件状态不一致
- 样本少导致错误学习

### 【下一轮交给 Codex 的任务清单】

请给出最多 10 项、可以直接执行的任务。每项必须包含：

- 任务目标
- 修改范围
- 不允许修改的范围
- 验证命令或验收信号
- 优先级

### 【最终验收标准】

请给出一套可以实际执行的验收清单，至少覆盖：

- 新建发布包
- 缺素材状态
- 缺视频状态
- 视频有效状态
- 标题违规状态
- 素材风险状态
- 人工发布确认
- 数据录入
- 反馈回流
- 下一条建议生成

---

## 十二、事实来源与人工确认边界

### 本文中由代码和本地文件真实读取确认的内容

- 项目目录与文件路径。
- 单包发布台页面结构和按钮。
- `video_ready` 判定逻辑。
- 微信视频号入口的启用条件。
- 标题 16 字、无空格、无标点规则。
- 剪贴板复制和回退逻辑。
- 发布前清单使用 `localStorage`。
- 综合工作台加载的 JSON 数据源。
- 综合工作台的发布前门禁、观察池、数据录入、学习、圆桌和雷达函数。
- `audit_rules.json` 的风险组。
- 最新发布包状态、图片数量、音乐路径和缺少 MP4 的事实。
- `radar_publish_bridge.json` 当前映射为空。
- 真实指标主要来自用户截图、CSV 或 JSON。
- 项目没有业务后端，使用 Python 静态服务器。
- `npm test` 没有真实测试。
- 发布台专用验证脚本本次运行通过。
- 最新发布包 JSON 和审核规则 JSON 本次解析通过。
- 项目根目录当前不是 Git 仓库。

### 需要人工确认的内容

- 最新 5 张图片是否审美合格、人物一致、无乱码、无 Logo、无水印。
- 候选音乐是否拥有可用授权。
- 最新内容是否值得发布，而不只是“材料齐全”。
- 最终 MP4 生成后的画面、音频、节奏、字幕安全区和结尾是否合格。
- 微信视频号后台实际发布是否成功。
- 发布后截图中的数据是否对应正确内容和时间节点。
- 用户晚上操作时是否真的感觉省力。
- 哪个页面应成为长期唯一入口。
- 哪些历史脚本、页面和实验可以归档或删除。

---

## 十三、当前扫描范围

本次重点扫描了以下目录：

- `E:\CodexProjects\videohao-content-factory\preview`
- `E:\CodexProjects\videohao-content-factory\preview\publish_desk_assets`
- `E:\CodexProjects\videohao-content-factory\scripts`
- `E:\CodexProjects\videohao-content-factory\data`
- `E:\CodexProjects\videohao-content-factory\data\metrics_inbox`
- `E:\CodexProjects\videohao-content-factory\data\imports`
- `E:\CodexProjects\videohao-content-factory\reports`
- `E:\CodexProjects\videohao-content-factory\publish_packages\pre_publish_prep`
- `E:\CodexProjects\videohao-content-factory\roundtable`
- 项目根目录的 `package.json`、`start_preview_server.bat`、`WORKFLOW.md`、`STATUS.md`

未将以下目录作为主扫描对象：

- `work/`：大量实验、依赖和缓存，只参考已有审计结论。
- `rendered_videos/`：历史成片较多，本次任务重点是发布台工程移交，不逐个媒体质检。
- `assets/`：只确认结构和相关素材路径，不逐张做视觉审核。

---

## 十四、审核前一句话结论

当前视频号发布台 MVP 已经能把“单条内容的发布准备信息”集中到一个本地页面，并能在缺少 MP4 时阻止进入人工发布入口；但整个增长工厂仍存在入口过多、状态来源不统一、验证体系分裂、真实数据样本少和闭环映射未建立的问题。下一轮最有价值的工作不是新增功能，而是确定唯一主入口、唯一状态源、统一发布包验证和真实的数据回流闭环。
